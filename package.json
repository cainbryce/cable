{
  "name": "cable",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "private": false,
  "description": "cain webframework",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run scripts/build.ts",
    "build:watch": "bun --watch run scripts/build.ts",
    "build:hot": "bun --hot run scripts/build.ts"
  },
  "devDependencies": {
    "bun-types": "latest",
    "@types/node": "latest"
  },
  "peerDependencies": {
    "typescript": "latest"
  }
}
